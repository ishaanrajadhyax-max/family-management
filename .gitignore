# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real personal health data (Dad's actual readings) — never commit,
# regardless of whether the repo is public or private.
/data/
/db/import/staging_blood_readings.csv

# Backend secrets — the DB password lives only here, never in git.
/server/.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.neon
